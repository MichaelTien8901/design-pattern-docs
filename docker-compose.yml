services:
  jekyll:
    build: .
    volumes:
      - .:/site
      - bundle-cache:/usr/local/bundle
    ports:
      - "4000:4000"
      - "35729:35729"
    working_dir: /site

volumes:
  bundle-cache:
